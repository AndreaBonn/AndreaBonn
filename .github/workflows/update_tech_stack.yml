name: Update Tech Stack

on:
  push:
    branches: [main]
    paths:
      - "scripts/tech_stack.py"
      - "scripts/tech_mappings.py"
      - "scripts/tech_svg.py"
      - "scripts/common/**"
      - "pyproject.toml"
      - "uv.lock"
  schedule:
    - cron: "0 2 * * 0"  # Sunday 02:00 UTC
  workflow_dispatch:

concurrency:
  group: update-tech-stack
  cancel-in-progress: true

jobs:
  update-tech-stack:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests
        run: uv run pytest -q

      - name: Generate Tech Stack SVG
        env:
          SNAKE_TOKEN: ${{ secrets.SNAKE_TOKEN }}
        run: uv run python scripts/tech_stack.py

      - name: Commit if changed
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/tech_stack.svg
          git diff --cached --quiet || git commit -m "chore: update tech stack $(date +'%Y-%m-%d') [skip ci]"
          git push
